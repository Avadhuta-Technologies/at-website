---
import Layout from '../layouts/Layout.astro';
import HeroSection from '../components/shared/HeroSection.astro';
import WhyChooseSection from '../components/packs/WhyChooseSection.astro';
import PacksHowItWorksSection from '../components/packs/PacksHowItWorksSection.astro';
import PackGrid from '../components/packs/PackGrid.astro';
import FAQSection from '../components/shared/FAQSection.astro';
import TestimonialsSection from '../components/shared/TestimonialsSection.astro';
import CTASection from '../components/shared/CTASection.astro';
import { packsData } from '../content/packs.js';
---

<Layout title="Explore Packs | NovaPod.ai" description="Browse fixed-scope packs to launch, grow, and automate your product.">
  <!-- Hero Section -->
  <section class="relative bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 overflow-hidden">
    <!-- Abstract Background Graphics -->
    <div class="absolute inset-0">
      <!-- Floating Orbs -->
      <div class="absolute top-20 left-10 w-32 h-32 bg-gradient-to-r from-blue-400 to-cyan-400 rounded-full opacity-20 animate-pulse"></div>
      <div class="absolute top-40 right-20 w-24 h-24 bg-gradient-to-r from-purple-400 to-pink-400 rounded-full opacity-30 animate-bounce"></div>
      <div class="absolute bottom-20 left-1/4 w-20 h-20 bg-gradient-to-r from-green-400 to-blue-400 rounded-full opacity-25 animate-ping"></div>
      
      <!-- Geometric Shapes -->
      <div class="absolute top-1/3 right-1/3 w-16 h-16 bg-gradient-to-r from-yellow-400 to-orange-400 transform rotate-45 opacity-20 animate-spin"></div>
      <div class="absolute bottom-1/3 left-1/3 w-12 h-12 bg-gradient-to-r from-red-400 to-pink-400 transform rotate-12 opacity-30"></div>
      
      <!-- Grid Pattern -->
      <div class="absolute inset-0 opacity-30" style="background-image: url('data:image/svg+xml,%3Csvg width=%2260%22 height=%2260%22 viewBox=%220 0 60 60%22 xmlns=%22http://www.w3.org/2000/svg%22%3E%3Cg fill=%22none%22 fill-rule=%22evenodd%22%3E%3Cg fill=%22%23ffffff%22 fill-opacity=%220.05%22%3E%3Ccircle cx=%2230%22 cy=%2230%22 r=%221%22/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');"></div>
    </div>

    <!-- Main Content -->
    <div class="relative z-10 container mx-auto px-4 py-20 md:py-32">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold text-white mb-6 leading-tight">
          <span class="bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400 bg-clip-text text-transparent">
            Add Power to Your Pod with Fixed-Scope Packs
          </span>
        </h1>
        <p class="text-xl md:text-2xl lg:text-3xl font-medium text-gray-300 mb-8 max-w-3xl mx-auto leading-relaxed">
          Modular Packs that extend your Pod's capabilities — from launching your product to automating operations, boosting growth, or integrating AI. Clear outcomes, fixed pricing, faster delivery.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <a href="#popular-packs" class="group relative px-8 py-4 bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold rounded-xl hover:from-blue-600 hover:to-purple-700 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
            <span class="relative z-10">Browse Packs</span>
            <div class="absolute inset-0 bg-gradient-to-r from-blue-600 to-purple-700 rounded-xl opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          </a>
          <a href="/contact" class="group px-8 py-4 border-2 border-gray-400 text-gray-300 font-semibold rounded-xl hover:border-white hover:text-white transition-all duration-300 transform hover:scale-105">
            <span class="flex items-center">Build Your Bundle</span>
          </a>
        </div>
      </div>
    </div>

    <!-- Bottom Wave -->
    <div class="absolute bottom-0 left-0 right-0 rotate-180">
      <svg class="w-full h-16 text-white" viewBox="0 0 1200 120" preserveAspectRatio="none">
        <path d="M0,0V46.29c47.79,22.2,103.59,32.17,158,28,70.36-5.37,136.33-33.31,206.8-37.5C438.64,32.43,512.34,53.67,583,72.05c69.27,18,138.3,24.88,209.4,13.08,36.15-6,69.85-17.84,104.45-29.34C989.49,25,1113-14.29,1200,52.47V0Z" opacity=".25" fill="currentColor"></path>
        <path d="M0,0V15.81C13,36.92,27.64,56.86,47.69,72.05,99.41,111.27,165,111,224.58,91.58c31.15-10.15,60.09-26.07,89.67-39.8,40.92-19,84.73-46,130.83-49.67,36.26-2.85,70.9,9.42,98.6,31.56,31.77,25.39,62.32,62,103.63,73,40.44,10.79,81.35-6.69,119.13-24.28s75.16-39,116.92-43.05c59.73-5.85,113.28,22.88,168.9,38.84,30.2,8.66,59,6.17,87.09-7.5,22.43-10.89,48-26.93,60.65-49.24V0Z" opacity=".5" fill="currentColor"></path>
        <path d="M0,0V5.63C149.93,59,314.09,71.32,475.83,42.57c43-7.64,84.23-20.12,127.61-26.46,59-8.63,112.48,12.24,165.56,35.4C827.93,77.22,886,95.24,951.2,90c86.53-7,172.46-45.71,248.8-84.81V0Z" fill="currentColor"></path>
      </svg>
    </div>
  </section>

  <!-- Why Choose Section -->
  <section class="py-16 bg-white dark:bg-slate-900">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold mb-6 text-dark dark:text-white">
          Why Choose a Fixed Scope Pack?
        </h2>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        <div class="bg-gray-50 dark:bg-slate-800 rounded-xl p-6 hover:shadow-lg transition-all duration-300">
          <div class="text-3xl mb-4">🚀</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Clear Scope, No Surprises</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Each Pack comes with well-defined deliverables, timelines, and pricing. No scope creep. No hourly billing drama.
          </p>
        </div>
        
        <div class="bg-gray-50 dark:bg-slate-800 rounded-xl p-6 hover:shadow-lg transition-all duration-300">
          <div class="text-3xl mb-4">⚡</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Faster Kickoff, Faster Results</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Skip endless meetings and proposals. Pick a Pack, pair it with your Pod, and get started within days.
          </p>
        </div>
        
        <div class="bg-gray-50 dark:bg-slate-800 rounded-xl p-6 hover:shadow-lg transition-all duration-300">
          <div class="text-3xl mb-4">💰</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Fixed Pricing = Predictable Budgeting</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Know exactly what you're paying for, upfront. Great for startups, finance teams, and anyone tired of inflated quotes.
          </p>
        </div>
        
        <div class="bg-gray-50 dark:bg-slate-800 rounded-xl p-6 hover:shadow-lg transition-all duration-300">
          <div class="text-3xl mb-4">🧩</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Modular & Stackable</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Start small and scale with confidence. Add more Packs as your needs grow — from launch to automation and beyond.
          </p>
        </div>
        
        <div class="bg-gray-50 dark:bg-slate-800 rounded-xl p-6 hover:shadow-lg transition-all duration-300">
          <div class="text-3xl mb-4">🤝</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Aligned Expectations</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Everyone's on the same page from day one — making collaboration smoother and delivery sharper.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works Section -->
  <section class="py-16 bg-gradient-to-br from-blue-50 to-purple-50 dark:from-blue-900/20 dark:to-purple-900/20">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold mb-6 text-dark dark:text-white">
          How It Works
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-400 mb-8">
          Simple steps to get started with a Pack:
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300">
          <div class="text-3xl mb-4">🧩</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Pick Your Pod</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Choose the service area that aligns with your goal — AI integration, automation, product launch, or more.
          </p>
        </div>
        
        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300">
          <div class="text-3xl mb-4">🎯</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Select a Pack</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Based on your needs and budget — Startup Pack, Scale Pack, or Custom — each one comes with defined deliverables and timelines.
          </p>
        </div>
        
        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300">
          <div class="text-3xl mb-4">🛠️</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Kick Off the Project</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Our team gets started with minimal handholding. You'll stay in the loop with regular updates and demos.
          </p>
        </div>
        
        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300">
          <div class="text-3xl mb-4">✅</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Review & Launch</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Once deliverables are met, we ensure everything is production-ready and handover is smooth.
          </p>
        </div>
        
        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300">
          <div class="text-3xl mb-4">➕</div>
          <h3 class="text-xl font-semibold mb-3 text-dark dark:text-white">Stack More Packs as You Scale</h3>
          <p class="text-gray-600 dark:text-gray-400">
            Extend, iterate, or switch focus by adding new Packs or Pods as your needs evolve.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Available Packs Section -->
  <section class="py-16 bg-white dark:bg-slate-900" id="popular-packs">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold mb-6 text-dark dark:text-white">
          All Packs
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-400">
          Browse our comprehensive collection of fixed-scope packs designed to accelerate your project outcomes
        </p>
      </div>
      
      {packsData.availablePacks.categories.map((cat) => (
        <div class="mb-16" id={cat.id}>
          <h3 class="text-2xl md:text-3xl font-bold mb-2 text-dark dark:text-white">{cat.title}</h3>
          <p class="text-gray-600 dark:text-gray-400 mb-6">{cat.description}</p>
          <PackGrid
            packs={cat.packs}
            learnMoreUrlPrefix="/packs"
          />
        </div>
      ))}
    </div>
  </section>

  <FAQSection 
    title={packsData.faq.title}
    subtitle={packsData.faq.subtitle}
    items={packsData.faq.items}
  />

  <!-- Testimonials Section -->
  <section class="py-16 bg-gradient-to-br from-gray-50 to-blue-50 dark:from-gray-900 dark:to-dark-2">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center mb-12">
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-6 text-dark dark:text-white animate-fade-in-up">
          {packsData.testimonials.title}
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-400 max-w-2xl mx-auto animate-fade-in-up animation-delay-200">
          {packsData.testimonials.subtitle}
        </p>
      </div>
      
      <!-- Testimonials Carousel -->
      <div class="max-w-4xl mx-auto relative">
        <div class="testimonials-carousel overflow-hidden">
          <div class="testimonials-track flex transition-transform duration-500 ease-in-out">
            {packsData.testimonials.items.map((testimonial, index) => (
              <div class="testimonial-slide w-full flex-shrink-0 px-4">
                <div class="bg-gradient-to-r from-blue-50 to-purple-50 dark:from-blue-900/20 dark:to-purple-900/20 p-8 rounded-2xl border border-blue-200 dark:border-blue-800">
                  <div class="flex items-start">
                    <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center mr-4 overflow-hidden">
                      <div class="w-full h-full bg-gradient-to-br from-blue-400 to-purple-500 flex items-center justify-center text-white font-bold text-lg">
                        {testimonial.authorName.split(' ').map(name => name[0]).join('').toUpperCase()}
                      </div>
                    </div>
                    <div class="flex-1">
                      <blockquote class="text-xl text-gray-700 dark:text-gray-300 italic mb-4 leading-relaxed">
                        "{testimonial.message}"
                      </blockquote>
                      <cite class="text-sm text-gray-600 dark:text-gray-400 font-medium">— {testimonial.authorName}</cite>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Navigation Dots -->
        <div class="flex justify-center mt-8 space-x-2">
          {packsData.testimonials.items.map((_, index) => (
            <button 
              class="testimonial-dot w-3 h-3 rounded-full bg-gray-300 dark:bg-gray-600 transition-all duration-300 hover:bg-blue-500 dark:hover:bg-blue-400"
              data-slide={index}
              aria-label={`Go to testimonial ${index + 1}`}
            ></button>
          ))}
        </div>
      </div>
    </div>
  </section>

  <style>
    .testimonials-carousel {
      position: relative;
    }
    
    .testimonials-track {
      display: flex;
      width: 100%;
    }
    
    .testimonial-slide {
      min-width: 100%;
    }
    
    .testimonial-dot.active {
      background-color: #3b82f6;
    }
    
    @media (prefers-reduced-motion: reduce) {
      .testimonials-track {
        transition: none;
      }
    }
  </style>

  <script>
    // Testimonials Carousel Functionality
    document.addEventListener('DOMContentLoaded', function() {
      const track = document.querySelector('.testimonials-track') as HTMLElement;
      const dots = document.querySelectorAll('.testimonial-dot');
      const slides = document.querySelectorAll('.testimonial-slide');
      
      if (!track || slides.length === 0) return;
      
      let currentSlide = 0;
      const totalSlides = slides.length;
      let autoplayInterval: any;
      
      // Initialize first dot as active
      if (dots.length > 0) {
        dots[0].classList.add('active');
      }
      
      // Function to go to specific slide
      function goToSlide(index: number) {
        currentSlide = index;
        const translateX = -index * 100;
        track.style.transform = `translateX(${translateX}%)`;
        
        // Update active dot
        dots.forEach((dot, i) => {
          dot.classList.toggle('active', i === index);
        });
      }
      
      // Function to go to next slide
      function nextSlide() {
        currentSlide = (currentSlide + 1) % totalSlides;
        goToSlide(currentSlide);
      }
      
      // Function to go to previous slide
      function prevSlide() {
        currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
        goToSlide(currentSlide);
      }
      
      // Add click handlers to dots
      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          goToSlide(index);
          resetAutoplay();
        });
      });
      
      // Autoplay functionality
      function startAutoplay() {
        autoplayInterval = setInterval(nextSlide, 5000); // Change slide every 5 seconds
      }
      
      function stopAutoplay() {
        if (autoplayInterval) {
          clearInterval(autoplayInterval);
        }
      }
      
      function resetAutoplay() {
        stopAutoplay();
        startAutoplay();
      }
      
      // Pause autoplay on hover
      const carousel = document.querySelector('.testimonials-carousel');
      if (carousel) {
        carousel.addEventListener('mouseenter', stopAutoplay);
        carousel.addEventListener('mouseleave', startAutoplay);
      }
      
      // Keyboard navigation
      document.addEventListener('keydown', (e) => {
        if (e.key === 'ArrowLeft') {
          prevSlide();
          resetAutoplay();
        } else if (e.key === 'ArrowRight') {
          nextSlide();
          resetAutoplay();
        }
      });
      
      // Touch/swipe support for mobile
      let startX = 0;
      let endX = 0;
      
      track.addEventListener('touchstart', (e: TouchEvent) => {
        startX = e.touches[0].clientX;
      });
      
      track.addEventListener('touchend', (e: TouchEvent) => {
        endX = e.changedTouches[0].clientX;
        handleSwipe();
      });
      
      function handleSwipe() {
        const threshold = 50;
        const diff = startX - endX;
        
        if (Math.abs(diff) > threshold) {
          if (diff > 0) {
            nextSlide();
          } else {
            prevSlide();
          }
          resetAutoplay();
        }
      }
      
      // Start autoplay
      startAutoplay();
    });
  </script>


</Layout>

<style>
  .animate-fade-in-up { animation: fadeInUp 0.8s ease-out; }
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }
</style> 