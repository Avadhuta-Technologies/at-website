---
import Layout from '../../layouts/Layout.astro';
import { catalogUtils } from '../../content/catalog/_index.js';
import { packsCatalog } from '../../content/catalog/_packs.js';

export async function getStaticPaths() {
  const { pods } = catalogUtils.getAllProducts();
  return pods.map((pod) => {
    // Generate slug from pod name
    const slug = pod.name.toLowerCase().replace(/[^a-z0-9]/g, '-').replace(/-+/g, '-').replace(/^-|-$/g, '');
    return {
      params: { slug },
      props: { pod },
    };
  });
}

const { pod } = Astro.props;

// Get relevant packs for this pod
const getRelevantPacks = (podName: string) => {
  const allPacks = packsCatalog.availablePacks;
  
  // Define pack recommendations based on pod type
  const packRecommendations: Record<string, string[]> = {
    'ecommerce-engine-pod': ['launch-pack', 'growth-pack', 'integration-pack'],
    'ar-vr-experience-pod': ['brand-starter-pack', 'web-presence-pack'],
    'ai-product-studio-pod': ['growth-pack', 'integration-pack'],
    'product-launch-pod': ['launch-pack', 'brand-starter-pack', 'web-presence-pack'],
    'admin-workflow-automation-pod': ['integration-pack', 'growth-pack'],
    'ai-ml-integration-pod': ['integration-pack', 'growth-pack'],
    'mobile-first-saas-pod': ['launch-pack', 'growth-pack', 'web-presence-pack'],
    'devops-infra-automation-pod': ['integration-pack']
  };
  
  // Generate slug from pod name for matching
  const podSlug = podName.toLowerCase().replace(/[^a-z0-9]/g, '-').replace(/-+/g, '-').replace(/^-|-$/g, '');
  
  // Get recommended pack IDs for this pod
  const recommendedPackIds = packRecommendations[podSlug] || ['launch-pack', 'growth-pack'];
  
  // Return the actual pack objects
  return allPacks.filter(pack => recommendedPackIds.includes(pack.id)).slice(0, 3);
};

const relevantPacks = getRelevantPacks(pod.name);
---

<Layout title={`${pod.name} - Pod Details`} description={pod.tagline}>
  <!-- Add class to body for dark header -->
  <script>
    document.body.classList.add('dark-header-page');
  </script>
  
  <!-- Hero Section -->
  <section class="relative bg-white overflow-hidden">
    <!-- Background Pattern -->
    <div class="absolute inset-0 bg-gray-50"></div>
    
    <!-- Decorative Elements -->
    <div class="absolute top-0 left-0 w-72 h-72 bg-primary-100 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-pulse"></div>
    <div class="absolute top-0 right-0 w-72 h-72 bg-purple-100 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-pulse animation-delay-2000"></div>
    
    <div class="relative z-10 container mx-auto px-4 py-20 md:py-32">
      <div class="max-w-4xl mx-auto text-center">
        
        <!-- Badge -->
        <div class="inline-flex items-center px-4 py-2 bg-primary-50 text-primary-700 text-sm font-semibold rounded-full mb-8 border border-primary-200">
          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
          </svg>
          Pod
        </div>
        
        <!-- Main Headline -->
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 mb-6 leading-tight">
          {pod.name}
        </h1>
        
        <!-- Subtitle -->
        <h2 class="text-xl md:text-2xl text-gray-600 mb-8 leading-relaxed max-w-3xl mx-auto">
          {pod.tagline}
        </h2>
        
        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-8">
          <button 
            data-add-to-cart
            data-item-id={pod.name.toLowerCase().replace(/[^a-z0-9]/g, '-').replace(/-+/g, '-').replace(/^-|-$/g, '')}
            data-item-type="pod"
            data-item-title={pod.name}
            data-item-description={pod.tagline}
            data-item-price={pod.pricing[0]}
            class="inline-flex items-center px-8 py-4 bg-primary-600 text-white font-semibold rounded-lg hover:bg-primary-700 transition-colors duration-200 group"
          >
            <span>Reserve Pod</span>
            <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </button>
          <a href="#details" class="inline-flex items-center px-8 py-4 bg-white text-gray-700 font-semibold rounded-lg border border-gray-300 hover:bg-gray-50 transition-colors duration-200 group">
            <span>Learn More</span>
            <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </a>
        </div>
        
        <!-- Stats -->
        <div class="flex items-center justify-center space-x-8 text-gray-500 text-sm">
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2 text-green-500" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
            4.9/5 Rating
          </div>
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2 text-green-500" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
            98% Success Rate
          </div>
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2 text-green-500" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
            2-6 Week Delivery
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Ideal For Section -->
  <section class="py-20 bg-gray-50" id="details">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Ideal For</h2>
        <p class="text-lg text-gray-600 leading-relaxed">Perfect for teams and projects that need specialized expertise</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {pod.idealFor.map((item: string, index: number) => (
          <div class="bg-white rounded-xl border border-gray-200 shadow-sm hover:shadow-md transition-all duration-300 hover:-translate-y-1 p-6">
            <div class="flex items-start">
              <div class="w-10 h-10 bg-primary-100 rounded-lg flex items-center justify-center mr-4 flex-shrink-0">
                <span class="text-primary-600 font-bold text-lg">{index + 1}</span>
              </div>
              <div>
                <p class="text-gray-800 leading-relaxed">{item}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- What's in the Pod Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">What's in the Pod?</h2>
        <p class="text-lg text-gray-600 leading-relaxed mb-8">{pod.whatsInPod.description}</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {pod.whatsInPod.teamComposition.map((member: string, index: number) => (
          <div class="bg-gray-50 rounded-xl p-6 border border-gray-200">
            <div class="flex items-start">
              <div class="w-12 h-12 bg-primary-100 rounded-lg flex items-center justify-center mr-4 flex-shrink-0">
                <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
              </div>
              <div>
                <p class="text-gray-600 leading-relaxed">{member}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
      
      {pod.whatsInPod.upgradeNote && (
        <div class="max-w-4xl mx-auto mt-12">
          <div class="bg-blue-50 border border-blue-200 rounded-xl p-6">
            <div class="flex items-start">
              <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center mr-4 flex-shrink-0">
                <svg class="w-4 h-4 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div>
                <h3 class="font-semibold text-blue-900 mb-2">Upgrade Note</h3>
                <p class="text-blue-800">{pod.whatsInPod.upgradeNote}</p>
              </div>
            </div>
          </div>
        </div>
      )}
    </div>
  </section>

  <!-- What You Get Section -->
  <section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">What You Get</h2>
        <p class="text-lg text-gray-600 leading-relaxed">Comprehensive deliverables for your project success</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {pod.whatCanDo.map((item: string, index: number) => (
          <div class="bg-white rounded-xl border border-gray-200 shadow-sm hover:shadow-md transition-all duration-300 hover:-translate-y-1 p-6">
            <div class="flex items-start mb-4">
              <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center mr-4 flex-shrink-0">
                <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
              </div>
            </div>
            <p class="text-gray-600 leading-relaxed">{item}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Delivery Timelines Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Delivery Timelines</h2>
        <p class="text-lg text-gray-600 leading-relaxed">Clear milestones and delivery schedule</p>
      </div>
      
      <div class="max-w-4xl mx-auto">
        <div class="space-y-6">
          {pod.deliveryTimelines.map((timeline: string, index: number) => (
            <div class="flex items-start">
              <div class="w-8 h-8 bg-primary-100 rounded-full flex items-center justify-center mr-4 flex-shrink-0 mt-1">
                <span class="text-primary-600 font-bold text-sm">{index + 1}</span>
              </div>
              <div class="bg-gray-50 rounded-xl p-6 flex-1 border border-gray-200">
                <p class="text-gray-800 leading-relaxed">{timeline}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Why Teams Trust This Pod Section -->
  <section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Why Teams Trust This Pod</h2>
        <p class="text-lg text-gray-600 leading-relaxed">Proven track record and expertise</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {pod.whyTrust.map((reason: string, index: number) => (
          <div class="bg-white rounded-xl border border-gray-200 shadow-sm hover:shadow-md transition-all duration-300 hover:-translate-y-1 p-6">
            <div class="flex items-start">
              <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center mr-4 flex-shrink-0">
                <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                </svg>
              </div>
              <div>
                <p class="text-gray-600 leading-relaxed">{reason}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Past Use Cases Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Past Use Cases</h2>
        <p class="text-lg text-gray-600 leading-relaxed">Real projects we've successfully delivered</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {pod.pastUseCases.map((useCase: string, index: number) => (
          <div class="bg-gray-50 rounded-xl p-6 border border-gray-200">
            <div class="flex items-start">
              <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center mr-4 flex-shrink-0">
                <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div>
                <h3 class="font-semibold text-gray-900 text-lg mb-2">Case Study {index + 1}</h3>
                <p class="text-gray-600 leading-relaxed">{useCase}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Pricing Section -->
  <section class="py-20 bg-gray-50" id="pricing">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Pricing</h2>
        <p class="text-lg text-gray-600 leading-relaxed">Transparent pricing with no hidden costs</p>
      </div>
      
      <div class="max-w-2xl mx-auto">
        <div class="bg-white rounded-2xl shadow-xl p-8 border border-gray-200">
          <div class="text-center mb-8">
            <div class="text-4xl font-bold text-gray-900 mb-2">{pod.pricing[0]}</div>
            {pod.pricing[1] && (
              <div class="text-gray-600 mb-4">{pod.pricing[1]}</div>
            )}
            <div class="inline-flex items-center px-4 py-2 bg-green-100 text-green-800 rounded-full text-sm font-semibold">
              <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Best Value Guarantee
            </div>
          </div>
          
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <button 
              data-add-to-cart
              data-item-id={pod.name.toLowerCase().replace(/[^a-z0-9]/g, '-').replace(/-+/g, '-').replace(/^-|-$/g, '')}
              data-item-type="pod"
              data-item-title={pod.name}
              data-item-description={pod.tagline}
              data-item-price={pod.pricing[0]}
              class="px-8 py-4 bg-primary-600 text-white font-semibold rounded-xl hover:bg-primary-700 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
              Reserve Pod
            </button>
            <a href="/contact" class="px-8 py-4 border-2 border-primary-600 text-primary-600 font-semibold rounded-xl hover:bg-primary-50 transition-all duration-300">Contact Us</a>
          </div>
          
          <div class="text-center mt-6">
            <p class="text-sm text-gray-500">⚡ Fast delivery • 🔒 Secure payment • 📞 24/7 support</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Add Packs to Your Pod Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Add Packs to Your Pod</h2>
        <p class="text-lg text-gray-600 leading-relaxed">Enhance your pod with specialized packs for additional features and capabilities</p>
      </div>
      
      <div class="max-w-6xl mx-auto">
        {relevantPacks.length > 0 ? (
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
            {relevantPacks.map((pack) => (
              <div class="bg-white rounded-xl border border-gray-200 shadow-sm hover:shadow-md transition-all duration-300 hover:-translate-y-1">
                <!-- Card Header -->
                <div class="p-6 border-b border-gray-100">
                  <div class="flex items-start justify-between mb-4">
                    <div class="w-12 h-12 bg-primary-50 rounded-lg flex items-center justify-center">
                      <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
                      </svg>
                    </div>
                    <span class={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${
                      pack.badgeColor === 'green' ? 'bg-green-100 text-green-800' :
                      pack.badgeColor === 'blue' ? 'bg-blue-100 text-blue-800' :
                      pack.badgeColor === 'purple' ? 'bg-purple-100 text-purple-800' :
                      pack.badgeColor === 'orange' ? 'bg-orange-100 text-orange-800' :
                      pack.badgeColor === 'indigo' ? 'bg-indigo-100 text-indigo-800' :
                      'bg-gray-100 text-gray-800'
                    }`}>
                      {pack.badge}
                    </span>
                  </div>
                  
                  <h3 class="text-xl font-semibold text-gray-900 mb-2">
                    {pack.title}
                  </h3>
                  
                  <div class="flex items-baseline space-x-2 mb-3">
                    <span class="text-2xl font-bold text-gray-900">
                      {pack.priceINR}
                    </span>
                  </div>
                  
                  <p class="text-sm text-gray-500 mb-4">
                    {pack.duration} • {pack.priceUSD}
                  </p>
                </div>
                
                <!-- Card Body -->
                <div class="p-6">
                  <p class="text-gray-600 mb-6 leading-relaxed">
                    {pack.description}
                  </p>
                  
                  <!-- Features List -->
                  <div class="space-y-3 mb-6">
                    {pack.deliverables.slice(0, 3).map((deliverable: string) => (
                      <div class="flex items-start">
                        <svg class="w-4 h-4 text-green-500 mr-3 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                        <span class="text-sm text-gray-600">{deliverable.split(' - ')[0]}</span>
                      </div>
                    ))}
                  </div>
                  
                  <div class="flex items-center justify-between">
                    <!-- CTA Button -->
                    <button 
                      data-add-to-cart
                      data-item-id={pack.id}
                      data-item-type="pack"
                      data-item-title={pack.title}
                      data-item-description={pack.description}
                      data-item-price={pack.priceINR}
                      class="w-full mr-1 inline-flex items-center justify-center px-4 py-2 bg-primary-600 text-white font-semibold rounded-lg hover:bg-primary-700 transition-colors duration-200">
                      Add Pack
                    </button>
                 
                    <a 
                      href={`/packs/${pack.id}`} 
                      class="w-full ml-1 inline-flex items-center justify-center px-4 py-2 bg-white border border-primary-600 text-primary-600 font-semibold rounded-lg hover:bg-primary-50 transition-colors duration-200"
                    >
                      View Details
                    </a>
                  </div>
                </div>
              </div>
            ))}
          </div>
        ) : (
          <div class="bg-gray-50 rounded-2xl p-8 border border-gray-200 text-center">
            <div class="w-16 h-16 bg-primary-100 rounded-2xl flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-2">Explore All Packs</h3>
            <p class="text-gray-600 mb-6">Discover our full range of specialized packs</p>
            <a href="/explore-packs" class="inline-flex items-center px-6 py-3 bg-primary-600 text-white font-semibold rounded-lg hover:bg-primary-700 transition-colors duration-200">
              Browse All Packs
              <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
              </svg>
            </a>
          </div>
        )}
        
        <div class="text-center">
          <a href="/explore-packs" class="inline-flex items-center px-6 py-3 bg-gray-100 text-gray-700 font-semibold rounded-lg hover:bg-gray-200 transition-colors duration-200">
            View All Available Packs
            <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Ready to Launch CTA Section -->
  <section class="py-20 bg-gray-900">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        
        <!-- Main Headline -->
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-6 leading-tight">
          Ready to Launch with This Pod?
        </h2>
        
        <!-- Subtitle -->
        <p class="text-xl md:text-2xl text-gray-300 mb-8 leading-relaxed max-w-3xl mx-auto">
          Start your project today and get your dedicated team of experts
        </p>
        
        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
          <button 
            data-add-to-cart
            data-item-id={pod.name.toLowerCase().replace(/[^a-z0-9]/g, '-').replace(/-+/g, '-').replace(/^-|-$/g, '')}
            data-item-type="pod"
            data-item-title={pod.name}
            data-item-description={pod.tagline}
            data-item-price={pod.pricing[0]}
            class="inline-flex items-center px-8 py-4 bg-primary-600 text-white font-semibold rounded-xl hover:bg-primary-700 transition-colors duration-200 group"
          >
            <span>Reserve Pod Now</span>
            <svg class="w-6 h-6 ml-2 group-hover:translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </button>
          
          <a 
            href="/contact"
            class="inline-flex items-center px-8 py-4 bg-white text-gray-900 font-semibold rounded-xl hover:bg-gray-100 transition-colors duration-200 group"
          >
            <span>Book Discovery Call</span>
            <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
            </svg>
          </a>
        </div>
        
        <!-- Contact Information -->
        <div class="bg-white/5 backdrop-blur-sm rounded-xl p-6 border border-white/10 max-w-md mx-auto">
          <div class="text-gray-300 mb-3 flex items-center justify-center">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
            Or reach us directly at:
          </div>
          <div class="space-y-2">
            <a 
              href="mailto:hello@avadhutatech.com"
              class="block text-blue-300 hover:text-blue-200 font-medium transition-colors duration-300"
            >
              📩 hello@avadhutatech.com
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .animate-fade-in { animation: fadeIn 0.6s ease-out; }
  .animate-fade-in-up { animation: fadeInUp 0.8s ease-out; }
  .animation-delay-200 { animation-delay: 0.2s; }
  .animation-delay-400 { animation-delay: 0.4s; }
  .animation-delay-600 { animation-delay: 0.6s; }
  @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
  @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
</style> 