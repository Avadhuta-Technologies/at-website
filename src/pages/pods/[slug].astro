---
import Layout from '../../layouts/Layout.astro';
import { podsData } from '../../content/pods.js';

export async function getStaticPaths() {
  return podsData.availablePods.pods.map((pod) => ({
    params: { slug: pod.id },
    props: { pod },
  }));
}

const { pod } = Astro.props;

// Utility functions for styling
function getBadgeStyle(color: string): string {
  const colorMap: Record<string, string> = {
    'blue': 'bg-blue-100 text-blue-800',
    'purple': 'bg-purple-100 text-purple-800',
    'orange': 'bg-orange-100 text-orange-800',
    'teal': 'bg-teal-100 text-teal-800',
    'green': 'bg-green-100 text-green-800',
    'indigo': 'bg-indigo-100 text-indigo-800',
    'cyan': 'bg-cyan-100 text-cyan-800',
    'pink': 'bg-pink-100 text-pink-800',
    'gray': 'bg-gray-100 text-gray-800'
  };
  return colorMap[color] || 'bg-blue-100 text-blue-800';
}

function getGradientClass(color: string): string {
  const colorMap: Record<string, string> = {
    'blue': 'from-blue-500 to-cyan-500',
    'purple': 'from-purple-500 to-pink-500',
    'orange': 'from-orange-500 to-red-500',
    'teal': 'from-teal-500 to-green-500',
    'green': 'from-green-500 to-emerald-500',
    'indigo': 'from-indigo-500 to-purple-500',
    'cyan': 'from-cyan-500 to-blue-500',
    'pink': 'from-pink-500 to-purple-500',
    'gray': 'from-gray-500 to-gray-600'
  };
  return colorMap[color] || 'from-blue-500 to-cyan-500';
}
---

<Layout title={`${pod.title} - Detailed Overview`} description={pod.description}>
  
  <!-- Hero Section with Urgency & Social Proof -->
  <section class="relative bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 overflow-hidden">
    <div class="absolute inset-0">
      <div class="absolute top-20 left-10 w-32 h-32 bg-gradient-to-r from-blue-400 to-cyan-400 rounded-full opacity-20 animate-pulse"></div>
      <div class="absolute top-40 right-20 w-24 h-24 bg-gradient-to-r from-purple-400 to-pink-400 rounded-full opacity-30 animate-bounce"></div>
      <div class="absolute bottom-20 left-1/4 w-20 h-20 bg-gradient-to-r from-green-400 to-blue-400 rounded-full opacity-25 animate-ping"></div>
    </div>

    <div class="relative z-10 container mx-auto px-4 py-20 md:py-32">
      <div class="max-w-4xl mx-auto text-center">
        {/* Social Proof Badge */}
        <div class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-green-500 to-emerald-500 text-white text-sm font-semibold rounded-full mb-4 animate-fade-in">
          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          Trusted by 50+ Companies
        </div>

        {/* Badge */}
        {pod.badge && (
          <div class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-purple-500 to-pink-500 text-white text-sm font-semibold rounded-full mb-8 animate-fade-in">
            {pod.badge}
          </div>
        )}

        <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold text-white mb-6 leading-tight animate-fade-in-up">
          <span class="bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400 bg-clip-text text-transparent">
            {pod.title}
          </span>
        </h1>
        
        <h2 class="text-xl md:text-2xl lg:text-3xl font-medium text-gray-300 mb-8 animate-fade-in-up animation-delay-200">
          {pod.description}
        </h2>

        {/* Urgency & Scarcity */}
        <div class="bg-red-500/20 border border-red-400/30 rounded-xl p-4 mb-8 animate-fade-in-up animation-delay-400">
          <p class="text-red-200 font-semibold">
            ⚡ Limited Availability: Only 2 slots remaining this month
          </p>
        </div>

        {/* Primary CTA with FOMO */}
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-8 animate-fade-in-up animation-delay-600">
          <a href="#contact" class="group relative px-8 py-4 bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold rounded-xl hover:from-blue-600 hover:to-purple-700 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
            <span class="relative z-10">Start Your Project Now</span>
            <div class="absolute inset-0 bg-gradient-to-r from-green-500 to-emerald-500 rounded-xl opacity-0 group-hover:opacity-100 transition-all duration-300"></div>
          </a>
          <a href="#details" class="px-8 py-4 border-2 border-white/30 text-white font-semibold rounded-xl hover:bg-white/10 transition-all duration-300">
            Learn More
          </a>
        </div>

        {/* Social Proof */}
        <div class="flex items-center justify-center space-x-8 text-gray-400 text-sm animate-fade-in-up animation-delay-800">
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
            <span>4.9/5 Rating</span>
          </div>
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
            <span>98% Success Rate</span>
          </div>
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
            <span>2-6 Week Delivery</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Pain Points & Solution Section -->
  <section class="py-16 bg-white" id="details">
    <div class="container mx-auto px-4">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-3xl md:text-4xl font-bold mb-6 text-gray-900">
            Stop Struggling with <span class="text-red-500">These Common Problems</span>
          </h2>
          <p class="text-lg text-gray-600 max-w-3xl mx-auto">
            Most teams face these challenges. Our {pod.title} solves them all.
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-16">
          {/* Pain Points */}
          <div class="space-y-6">
            <h3 class="text-2xl font-bold text-red-600 mb-8">❌ What You're Dealing With</h3>
            <div class="space-y-4">
              <div class="bg-red-50 border-l-4 border-red-500 p-6 rounded-r-xl">
                <h4 class="font-semibold text-red-800 mb-2">Hiring Delays & Headaches</h4>
                <p class="text-red-700">2-3 months to hire, onboard, and get productive. Time you don't have.</p>
              </div>
              <div class="bg-red-50 border-l-4 border-red-500 p-6 rounded-r-xl">
                <h4 class="font-semibold text-red-800 mb-2">Skill Gaps & Inconsistency</h4>
                <p class="text-red-700">Different skill levels, communication issues, and project delays.</p>
              </div>
              <div class="bg-red-50 border-l-4 border-red-500 p-6 rounded-r-xl">
                <h4 class="font-semibold text-red-800 mb-2">High Costs & Low ROI</h4>
                <p class="text-red-700">Salaries, benefits, infrastructure costs eating into your budget.</p>
              </div>
            </div>
          </div>
          
          {/* Solutions */}
          <div class="space-y-6">
            <h3 class="text-2xl font-bold text-green-600 mb-8">✅ How {pod.title} Solves It</h3>
            <div class="space-y-4">
              <div class="bg-green-50 border-l-4 border-green-500 p-6 rounded-r-xl">
                <h4 class="font-semibold text-green-800 mb-2">Ready-to-Start Team</h4>
                <p class="text-green-700">Pre-vetted specialists start within 1 week. No delays, no headaches.</p>
              </div>
              <div class="bg-green-50 border-l-4 border-green-500 p-6 rounded-r-xl">
                <h4 class="font-semibold text-green-800 mb-2">Proven Team Chemistry</h4>
                <p class="text-green-700">Tested team combinations that work together seamlessly.</p>
              </div>
              <div class="bg-green-50 border-l-4 border-green-500 p-6 rounded-r-xl">
                <h4 class="font-semibold text-green-800 mb-2">Predictable Costs</h4>
                <p class="text-green-700">One monthly fee. No hidden costs, no surprises.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Ideal For Section with Emotional Appeal -->
  <section class="py-16 bg-gradient-to-br from-blue-50 to-indigo-50">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
            Perfect For <span class="text-blue-600">Ambitious Teams</span>
          </h2>
          <p class="text-lg text-gray-600">If you're serious about results, this pod is for you</p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {pod.idealFor.map((item, index) => (
            <div class="group relative bg-white rounded-2xl p-8 border border-blue-100 transform transition-all duration-500 hover:scale-105 hover:shadow-2xl hover:border-blue-300">
              <div class="absolute top-4 right-4 w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 transition-all duration-300">
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div class="flex items-center mb-4">
                <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-purple-600 rounded-xl flex items-center justify-center mr-4 transform transition-all duration-300 group-hover:scale-110 group-hover:rotate-3">
                  <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                  </svg>
                </div>
                <span class="font-bold text-gray-900 text-lg">Perfect Match</span>
              </div>
              <p class="text-gray-700 text-lg leading-relaxed">{item}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Team Composition with Trust Building -->
  <section class="py-16 bg-white">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
            Meet Your <span class="text-purple-600">Dedicated Team</span>
          </h2>
          <p class="text-lg text-gray-600">Expert specialists working exclusively on your project</p>
        </div>
        
        <div class="bg-gradient-to-br from-purple-50 to-pink-50 rounded-2xl shadow-lg p-8 transform transition-all duration-700 hover:shadow-2xl hover:-translate-y-2">
          <h3 class="text-2xl font-bold mb-8 text-gray-900 flex items-center">
            <div class="w-8 h-8 bg-gradient-to-r from-purple-500 to-pink-600 rounded-lg flex items-center justify-center mr-4">
              <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
            </div>
            Your Team Composition
          </h3>
          <div class="space-y-4">
            {pod.teamComposition.map((member, index) => (
              <div class="flex items-start transform transition-all duration-300 hover:translate-x-2 group">
                <div class="w-3 h-3 bg-gradient-to-r from-purple-500 to-pink-600 rounded-full mt-3 mr-4 flex-shrink-0 transition-all duration-300 group-hover:scale-150 group-hover:shadow-lg"></div>
                <div class="flex-1 bg-white rounded-lg p-4 border-l-4 border-purple-500 transition-all duration-300 group-hover:bg-purple-50 group-hover:border-purple-600">
                  <p class="text-gray-700 font-medium">{member}</p>
                </div>
              </div>
            ))}
          </div>
          
          {pod.upgradeNote && (
            <div class="mt-8 p-6 bg-gradient-to-r from-yellow-50 to-orange-50 rounded-xl border border-yellow-200 transform transition-all duration-500 hover:scale-105 hover:shadow-lg">
              <div class="flex items-center">
                <div class="w-8 h-8 bg-gradient-to-r from-yellow-500 to-orange-500 rounded-lg flex items-center justify-center mr-4">
                  <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                  </svg>
                </div>
                <p class="text-yellow-800 font-semibold text-lg">{pod.upgradeNote}</p>
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  </section>

  <!-- Deliverables with Value Proposition -->
  <section class="py-16 bg-gradient-to-br from-green-50 to-emerald-50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
          What You <span class="text-green-600">Actually Get</span>
        </h2>
        <p class="text-lg text-gray-600">No fluff, just real deliverables that drive results</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {pod.deliverables.map((deliverable, index) => (
          <div class="group relative bg-white rounded-2xl p-8 border border-green-200 transform transition-all duration-500 hover:scale-105 hover:shadow-2xl hover:border-green-400 hover:-translate-y-2">
            <div class="absolute top-4 right-4 w-8 h-8 bg-gradient-to-r from-green-500 to-emerald-600 rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 transition-all duration-300">
              <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <div class="flex items-center mb-4">
              <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-emerald-600 rounded-xl flex items-center justify-center mr-4 transform transition-all duration-300 group-hover:scale-110 group-hover:rotate-3">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
              </div>
              <span class="font-bold text-gray-900 text-lg">Included</span>
            </div>
            <p class="text-gray-700 text-lg leading-relaxed">{deliverable}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Timeline with Urgency -->
  {pod.deliveryTimelines && pod.deliveryTimelines.length > 0 && (
    <section class="py-16 bg-white">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
            Fast <span class="text-orange-600">Delivery Timeline</span>
          </h2>
          <p class="text-lg text-gray-600">Quick wins, fast results, no delays</p>
        </div>
        
        <div class="max-w-4xl mx-auto">
          <div class="relative">
            <div class="absolute left-8 top-0 bottom-0 w-1 bg-gradient-to-b from-orange-500 to-red-500"></div>
            <div class="space-y-8">
              {pod.deliveryTimelines.map((timeline, index) => (
                <div class="relative flex items-center">
                  <div class="absolute left-6 w-4 h-4 bg-gradient-to-r from-orange-500 to-red-500 rounded-full border-4 border-white shadow-lg"></div>
                  <div class="ml-16 bg-gradient-to-r from-orange-50 to-red-50 rounded-xl p-6 border border-orange-200 transform transition-all duration-300 hover:scale-105 hover:shadow-lg">
                    <h3 class="font-bold text-orange-800 text-lg mb-2">{timeline}</h3>
                    <p class="text-orange-700">Milestone {index + 1}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
  )}

  <!-- Trust Factors with Social Proof -->
  {pod.whyTrust && pod.whyTrust.length > 0 && (
    <section class="py-16 bg-gradient-to-br from-purple-50 to-pink-50">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
            Why Teams <span class="text-purple-600">Trust This Pod</span>
          </h2>
          <p class="text-lg text-gray-600">Proven track record and reliable delivery</p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 max-w-6xl mx-auto">
          {pod.whyTrust.map((trust, index) => (
            <div class="bg-white rounded-xl p-6 shadow-lg transform transition-all duration-500 hover:scale-105 hover:shadow-2xl hover:-translate-y-2">
              <div class="w-12 h-12 bg-gradient-to-r from-purple-500 to-pink-600 rounded-lg flex items-center justify-center mb-4 transform transition-all duration-300 group-hover:scale-110 group-hover:rotate-3">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                </svg>
              </div>
              <p class="text-gray-700 font-medium">{trust}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- Past Success Stories -->
  {pod.pastUseCases && pod.pastUseCases.length > 0 && (
    <section class="py-16 bg-white">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
            Real <span class="text-indigo-600">Success Stories</span>
          </h2>
          <p class="text-lg text-gray-600">See how other teams achieved their goals</p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
          {pod.pastUseCases.map((useCase, index) => (
            <div class="bg-gradient-to-br from-indigo-50 to-purple-50 rounded-2xl p-8 border border-indigo-200 transform transition-all duration-500 hover:scale-105 hover:shadow-2xl hover:border-indigo-400">
              <div class="flex items-center mb-4">
                <div class="w-12 h-12 bg-gradient-to-r from-indigo-500 to-purple-600 rounded-xl flex items-center justify-center mr-4">
                  <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
                  </svg>
                </div>
                <span class="font-bold text-gray-900 text-lg">Case Study</span>
              </div>
              <p class="text-gray-700 text-lg leading-relaxed">{useCase}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- Pricing with Value Justification -->
  <section class="py-16 bg-gradient-to-br from-gray-50 to-blue-50" id="contact">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
            Investment & <span class="text-blue-600">ROI</span>
          </h2>
          <p class="text-lg text-gray-600">Transparent pricing with guaranteed value</p>
        </div>
        
        <div class="bg-white rounded-2xl shadow-2xl p-8 transform transition-all duration-500 hover:shadow-3xl">
          <div class="text-center mb-8">
            <div class="text-4xl font-bold text-gray-900 mb-2">{pod.engagement}</div>
            <p class="text-gray-600 mb-4">{pod.priceNote}</p>
            <div class="inline-flex items-center px-4 py-2 bg-green-100 text-green-800 rounded-full text-sm font-semibold">
              <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Best Value Guarantee
            </div>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <div class="text-center p-4">
              <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-3">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
                </svg>
              </div>
              <h3 class="font-semibold text-gray-900 mb-2">Cost Savings</h3>
              <p class="text-gray-600 text-sm">Save 40-60% vs hiring in-house</p>
            </div>
            <div class="text-center p-4">
              <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-3">
                <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                </svg>
              </div>
              <h3 class="font-semibold text-gray-900 mb-2">Fast Results</h3>
              <p class="text-gray-600 text-sm">Start within 1 week, not 3 months</p>
            </div>
            <div class="text-center p-4">
              <div class="w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-3">
                <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <h3 class="font-semibold text-gray-900 mb-2">Risk-Free</h3>
              <p class="text-gray-600 text-sm">30-day money-back guarantee</p>
            </div>
          </div>
          
          {/* Final CTA with Multiple Options */}
          <div class="text-center space-y-4">
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
              <a href="/contact" class="px-8 py-4 bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold rounded-xl hover:from-blue-600 hover:to-purple-700 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
                Start Your Project Now
              </a>
              <a href="/contact" class="px-8 py-4 border-2 border-blue-500 text-blue-600 font-semibold rounded-xl hover:bg-blue-50 transition-all duration-300">
                Schedule a Call
              </a>
            </div>
            <p class="text-sm text-gray-500">
              ⚡ Limited availability • 🔒 Secure payment • 📞 24/7 support
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

</Layout>

<style>
  .animate-fade-in {
    animation: fadeIn 0.6s ease-out;
  }
  
  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out;
  }
  
  .animation-delay-200 { animation-delay: 0.2s; }
  .animation-delay-400 { animation-delay: 0.4s; }
  .animation-delay-600 { animation-delay: 0.6s; }
  .animation-delay-800 { animation-delay: 0.8s; }
  
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  
  @keyframes fadeInUp {
    from { 
      opacity: 0;
      transform: translateY(30px);
    }
    to { 
      opacity: 1;
      transform: translateY(0);
    }
  }
</style> 